# SPDX-FileCopyrightText: Copyright (c) 2023 - 2026 NVIDIA CORPORATION & AFFILIATES.
# SPDX-FileCopyrightText: All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# DEC ablation — arm "both": DEC shape features enter through BOTH the per-node
# fx stream AND the geometry-context input. Compare against the baseline
# bumper_geotransolver_oneshot.yaml and the _dec_ctx / _dec_local arms with
# identical data, splits, and seeds.
# Usage: python train.py --config-name=bumper_geotransolver_oneshot_dec_both

hydra:
  job:
    chdir: True
  run:
    dir: ./outputs/

experiment_name: "Bumper-GeoTransolver-DEC-both"

defaults:
  - reader: vtkhdf
  - datapipe: point_cloud
  - model: geotransolver_one_shot
  - training: default
  - inference: default
  - _self_

reader:
  dec_features: true

# ┌───────────────────────────────────────────┐
# │                   Data                    │
# └───────────────────────────────────────────┘

training:
  raw_data_dir: ???        # set in config or via CLI: training.raw_data_dir=/path/to/train
  raw_data_dir_validation: ???   # set in config or via CLI: training.raw_data_dir_validation=/path/to/validation
  global_features_filepath: ???  # set in config or via CLI: training.global_features_filepath=/path/to/global_features.json
  optimizer: muon

  num_time_steps: 101
  num_training_samples: 121
  num_validation_samples: 5

inference:
  raw_data_dir_test: ???         # set in config or via CLI: inference.raw_data_dir_test=/path/to/test

# ┌───────────────────────────────────────────┐
# │            Datapipe features              │
# └───────────────────────────────────────────┘

datapipe:
  static_features:               # → fx = cat(coords, these) [N, 3+7]
    - dec_h_signed
    - dec_h_mag
    - dec_normal
    - dec_log_area
    - dec_is_boundary
  geometry_features:             # → geometry = cat(coords, these) [N, 3+7]
    - dec_h_signed
    - dec_h_mag
    - dec_normal
    - dec_log_area
    - dec_is_boundary
  dynamic_targets:
    - effective_plastic_strain
    - stress_vm
  global_features:
    - velocity_x
    - thickness_scale
    - rwall_origin_y
  sample_type: all_time_steps
  stats_dir: stats_dec_both       # feature dims differ from baseline → own stats

# ┌───────────────────────────────────────────┐
# │            Model overrides                │
# └───────────────────────────────────────────┘

model:
  functional_dim: 10  # coords (3) + DEC features (7)
  geometry_dim: 10    # coords (3) + DEC features (7)
  out_dim: 500        # (num_time_steps - 1) * 5 = 50 * 5
  global_dim: 3       # must match len(datapipe.global_features)
  guard_config: null
